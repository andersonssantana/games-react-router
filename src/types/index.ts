export type Game = {
  id: string;
  name: string;
  genre: string;
  story: string;
  gameplay: string;
  developer: string;
  publisher: string;
  platforms: string[];
};

// export type Plataforms = ['Nintendo Switch' | 'PC' | 'Playstation 5'];
